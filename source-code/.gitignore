# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
.eggs/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Virtual environments
venv/
.venv/
env/
ENV/

# Packaging (PyInstaller / WiX)
build/
dist/
packaging/vendor/
packaging/version_info.txt
*.wixpdb
*.wixobj

# The published installer is versioned in the repository root, one level above
# this folder, so it shows up in the listing. Note: GitHub rejects files over
# 100 MB, and every version added stays in the history forever.

# Compiled translations — generated from the .ts files by
# packaging/build_translations.ps1, which the build and CI both run.
*.qm

# Local data written by the app
*.log
settings.json
history.json

# Editors / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini
